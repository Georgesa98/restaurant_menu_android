# Template for local-dev overrides. Copy to `.env`:
#   cp .env.example .env
# Resolution order per key: explicit --dart-define > .env > compiled default.
# `.env` is gitignored; this file is the committed reference.
MENU_API_URL=https://menu.georgesalebe.me
TENANT_SLUG=demo
TENANT_ID=
TENANT_NAME=Demo Restaurant
